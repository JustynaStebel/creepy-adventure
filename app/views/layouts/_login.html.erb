<% if current_user %>
  Signed in!
  <%= link_to "Logout", destroy_user_session_path, method: :delete %>
<% else %>
  <h3 class="login">Log in or register to create your movie collection for free!</h3>
  <div class="register">
    <%= link_to "Register", new_user_registration_path %>
  </div>
  <div class="log">
    <%= link_to "Login", new_user_session_path %>
  </div>
<% end %>
